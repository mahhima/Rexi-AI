{
  "permissions": {
    "allow": [
      "Bash(pkill -f \"vite --port 5174\")",
      "Bash(pkill -f \"uvicorn app.main\")",
      "Bash(python3 -m uvicorn app.main:app --port 8000 --reload)",
      "Bash(node -e ' *)"
    ]
  }
}
